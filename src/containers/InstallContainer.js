import React from 'react';
import styled from 'styled-components';

const Wrapper = styled('div')`
  display: flex;
`;

export default function InstallContainer() {
  return (
    <Wrapper>
      <div>설치 하는 방법 안내 페이지</div>
    </Wrapper>
  );
}
